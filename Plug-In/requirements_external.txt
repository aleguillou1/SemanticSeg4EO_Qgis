# SemanticSeg4EO - External Environment Requirements
# ==================================================
#
# Install these in your Conda/venv environment, NOT in QGIS!
#
# Quick install:
#   pip install -r requirements_external.txt
#
# For GPU support (RTX 3080, etc), install PyTorch with CUDA:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# Then:
#   pip install -r requirements_external.txt

# Core (required)
torch>=1.10.0
torchvision>=0.11.0
numpy>=1.21.0
rasterio>=1.2.0

# Geospatial (required for patch extraction)
geopandas>=0.10.0
opencv-python>=4.5.0

# Deep Learning Models (highly recommended)
segmentation-models-pytorch>=0.3.0

# Visualization (for training plots)
matplotlib>=3.4.0

# Optional (for reading compressed TIFF files in training)
tifffile>=2021.7.2
imagecodecs>=2021.8.0

# Utilities
scipy>=1.7.0
tqdm>=4.62.0
